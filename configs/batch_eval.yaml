# Hydra config for batch evaluation used by superoptim/batch_evaluate.py
type: iou
prefix: shapenet/shapenet_test
small: false
num_epochs: 1000
device: cuda

optimization:
  tapering: false
  bending: false
  reorient: false
  pruning: false

dataloader:
  dataset: shapenet
  batch_size: 256
  num_workers: 4

shapenet:
  split: test
  path: data/ShapeNet
  categories: null
  load_occupancy: true
  use_fps: true
